import {create} from 'react-native-pixel-perfect';

/**
 * Provide the width & height of the screen size used in design in pixels.
 */
const dimensions = {
  width: 1125,
  height: 2436,
};

export const pixelSize = create(dimensions);
